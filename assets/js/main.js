AOS.init({

});

//document.addEventListener("DOMContentLoaded", () => {
//  const dropdownToggles = document.querySelectorAll('.navbar-nav .dropdown-toggle');
  
//  const handleResponsiveDropdowns = () => {
 //   if (window.innerWidth < 1200) {
//      dropdownToggles.forEach(el => el.setAttribute('data-bs-toggle', 'dropdown'));
//    } else {
//      dropdownToggles.forEach(el => el.removeAttribute('data-bs-toggle'));
//    }
//  };

  // Run on load and whenever screen resizes
 // handleResponsiveDropdowns();
 // window.addEventListener('resize', handleResponsiveDropdowns);
//});
